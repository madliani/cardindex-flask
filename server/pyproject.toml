[project]
description = "A server part of the Post project."
name = "post-server"
readme = "README.md"
requires-python = ">=3.14"
version = "0.1.0"

dependencies = [
    "flask>=3.1.3",
]

[dependency-groups]
dev = [
    "invoke>=3.0.3",
    "pytest>=9.1.1",
]
